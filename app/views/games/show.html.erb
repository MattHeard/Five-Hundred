<p>North's hand:</p>
<ul>
  <% @game_state.hands[:north].each do |_| %>
    <li><%= unicode_card("BACK") %></li>
  <% end %>
</ul>
<p>West's hand:</p>
<ul>
  <% @game_state.hands[:west].each do |_| %>
    <li><%= unicode_card("BACK") %></li>
  <% end %>
</ul>
<p>South's hand:</p>
<ul>
  <% @game_state.hands[:south].each do |card| %>
    <li><%= unicode_card(card) %></li>
  <% end %>
</ul>
<p>East's hand:</p>
<ul>
  <% @game_state.hands[:east].each do |_| %>
    <li><%= unicode_card("BACK") %></li>
  <% end %>
</ul>
<p>The kitty:</p>
<ul>
  <% @game_state.kitty.each do |_| %>
    <li><%= unicode_card("BACK") %></li>
  <% end %>
</ul>
<p>The dealer is <%= @game_state.dealer %>.</p>
<p>The current bid is <%= @game_state.highest_bid.nil? ? "nothing" : pretty_bid(@game_state.highest_bid) %>.</p>
<% if @game_state.in_bidding_phase? %>
  <p>The bidder is <%= @game_state.bidder %>.</p>
  <p>It is <%= @game_state.bidder %>'s turn to make a bid:</p>
  <%= form_for @game, url: { action: :bid } do %>
    <select name="bid_or_pass">
      <option>Bid</option>
      <option>Pass</option>
    </select>
    <select name="number_of_tricks">
      <option>6</option>
      <option>7</option>
      <option>8</option>
    </select>
    <select name="trump_suit">
      <option>♠</option>
    </select>
    <button type="submit">Submit</button>
  <% end %>
<% end %>
