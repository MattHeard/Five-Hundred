<p>Let's play 500!</p>
<p>First, we will deal the cards.</p>
<p>North's hand:</p>
<ul>
  <% @game_state.hands[:north].each do |_| %>
    <li><%= unicode_card("BACK") %></li>
  <% end %>
</ul>
<p>West's hand:</p>
<ul>
  <% @game_state.hands[:west].each do |_| %>
    <li><%= unicode_card("BACK") %></li>
  <% end %>
</ul>
<p>South's hand:</p>
<ul>
  <% @game_state.hands[:south].each do |card| %>
    <li><%= unicode_card(card) %></li>
  <% end %>
</ul>
<p>East's hand:</p>
<ul>
  <% @game_state.hands[:east].each do |_| %>
    <li><%= unicode_card("BACK") %></li>
  <% end %>
</ul>
<p>The kitty:</p>
<ul>
  <% @game_state.kitty.each do |_| %>
    <li><%= unicode_card("BACK") %></li>
  <% end %>
</ul>
<p>It is now the bidding phase.</p>
<p>The dealer is <%= @game_state.dealer %>.</p>
<p>The bidder is <%= @game_state.bidder %>.</p>
<p>It is west's turn to make a bid:</p>
<%= form_for @game, url: { action: :update } do %>
  <select>
    <option>Pass</option>
  </select>
  <button type="submit">Submit</button>
<% end %>
